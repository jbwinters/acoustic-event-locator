[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --tb=short --strict-markers --durations=5
markers =
    slow: tests that take more than a few seconds
filterwarnings =
    ignore::DeprecationWarning
